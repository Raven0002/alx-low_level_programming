#include "main.h"

/**
 * _memset - function to set memory to array
 * @n: number of bytes
 * @s: string
 * @b: constant byte
 *
 * Return: s
 */
char *_memset(char *s, char b, unsigned int n)
{
	unsigned int i = O;

	while (i < n)
	{
	s[i] = b
	i++;
	}

	return (s);
}
